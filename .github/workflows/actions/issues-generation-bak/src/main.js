/*
return github.paginate("GET /search/issues", {
        q: `repo:${repository} is:pr is:merged milestone:${milestone}`,
      })
*/

module.exports = {
  main: function (github, event) {
    
  },

  normalizeComment: function(event) {

  }

  parseComment: function(comment) {
    
  }
}